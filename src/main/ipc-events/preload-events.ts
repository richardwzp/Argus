// eslint-disable-next-line import/prefer-default-export
export enum Channels {
  IPC_EXAMPLE = 'ipc-example',
  TIMES_UP = 'times-up',
  STARTING_SECONDS = 'starting-seconds',
  RESTART_TIMER = 'restart-timer',
}
